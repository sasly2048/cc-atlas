---
description: Point the user to cc-atlas's full interactive menu (dashboard, project comparison, session replay, team activity, anomalies, goals) — screens that need a real terminal.
disable-model-invocation: true
---

Tell the user to run `cc-atlas` directly in their own terminal (not
through this chat) to open the interactive menu. That's where the
Dashboard, Compare two projects, Session replay, Team activity,
Anomalies, and Goals screens live — they need a real TTY for interactive
prompts and can't be driven through a tool call.
